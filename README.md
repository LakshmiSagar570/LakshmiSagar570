<div align="center">

```
$ whoami
sagar — B.Tech CS (AI & ML), builds things until they break, then again until they don't
```

</div>

<br>

```bash
$ cat currently.log
```

- Compressing an ML ensemble (XGBoost + RandomForest, 0.9992 ROC-AUC) to run on constrained hardware — no GPU, no shortcuts
- Building an LLM inference server from scratch: block manager → scheduler → chunked prefill → custom Triton attention kernel → REST layer → agentic scheduling
- Camera-ready fixes on an IEEE paper — the kind of bugs that live in tables and citations instead of code

<br>

```bash
$ ls shipped/
```

| project | what it does | stack |
|---|---|---|
| **DNS Sentinel** | DNS tunneling detection, ensemble ML, cyberpunk landing page | XGBoost, RandomForest, PyInstaller |
| **AssumptionX** | live product — [assumption-x.vercel.app](https://assumption-x.vercel.app) | Next.js, Clerk, Supabase, OpenRouter |
| **PatchPilot** | self-healing test agent, dual-LLM log analysis | FastAPI, Groq, Gemini |
| **LFAN** | lightweight super-resolution CNN — 731K params, 78ms inference | PyTorch |

<br>

```bash
$ cat stack.txt
```

`Python` `FastAPI` `Next.js` `PostgreSQL` `Docker` `LangChain` `XGBoost`

<br>

```bash
$ tail -f log.txt
```

no torchbearer arcs here — just a running log of things built, broken, and fixed.

<br>

<div align="center">

[![GitHub](https://img.shields.io/badge/GitHub-LakshmiSagar570-181717?style=flat-square&logo=github)](https://github.com/LakshmiSagar570)

</div>
